import os

import pandas as pd


def test(a: int, b: float, c: str, d: pd.DataFrame) -> int:
    """_summary_

    Args:
        a (int): _description_
        b (float): _description_
        c (str): _description_
        d (pd.DataFrame): _description_

    Returns:
        int: _description_

    """
    return 0


os.abort
a = pd.DataFrame()
